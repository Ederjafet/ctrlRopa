# Frontend Guidelines
- Use theme everywhere
- Consistent back button
- Do not expose manual order creation
- Follow flows strictly
- Do not expose manual item status editing
- Allow item property editing only for AVAILABLE items
- Door Sale must not send the user to package creation; it ends after successful paid sale
